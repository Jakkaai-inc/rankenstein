# Point the app at your API. Precedence: this var > app.json extra.apiBase > localhost.
#
# iOS simulator can use localhost; a physical device must use your machine's LAN
# IP (e.g. http://192.168.1.20:3000) or the deployed URL.
EXPO_PUBLIC_API_BASE=https://rankenstein.app
